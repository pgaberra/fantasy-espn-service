package com.fantasy.espn.league.dto;

/** Where a league's draft stands, read from ESPN's {@code draftDetail}. */
public enum DraftStatus {
    PRE_DRAFT,
    IN_PROGRESS,
    FINISHED,
    UNKNOWN
}
